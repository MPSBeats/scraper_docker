apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: restaurant-api
  labels:
    app: postgres
spec:
  type: ClusterIP
  clusterIP: None  # Headless service pour StatefulSet
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres
